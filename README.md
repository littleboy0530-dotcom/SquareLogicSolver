# SquaerLogicSolver

A Rust-based Nonogram (Picross) solver using DP-based candidate enumeration and backtracking.

Usage:

- Build: `cargo build --release`
- Run: `cargo run`

Contributions welcome.
